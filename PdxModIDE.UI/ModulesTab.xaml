<UserControl x:Class="PdxModIDE.UI.ModulesTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource ModulesTab_CurrentGame}" FontWeight="SemiBold" Grid.Row="0" Grid.ColumnSpan="2"/>
        <TextBlock x:Name="GameLabel" Text="{Binding CurrentProfile.Game}" Foreground="Gray" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,25,0,5"/>

        <ListBox x:Name="ModuleList" ItemsSource="{Binding Modules}" SelectedItem="{Binding SelectedModule, Mode=TwoWay}" Grid.Row="2" Grid.Column="0" Margin="0,5,10,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,5,0,0">
            <GroupBox Header="{DynamicResource ModulesTab_EditModule}" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="{DynamicResource ModulesTab_ModuleName}" Margin="0,5,0,2"/>
                    <TextBox x:Name="EntryName" Text="{Binding SelectedModule.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>
                    <TextBlock Text="{DynamicResource ModulesTab_RelativePath}" Margin="0,5,0,2"/>
                    <TextBox x:Name="EntryPath" Text="{Binding SelectedModule.Path, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>
                    <TextBlock Text="{DynamicResource ModulesTab_IgnoredExt}" Margin="0,5,0,2"/>
                    <TextBox x:Name="EntryIgnore" Text="{Binding SelectedModule.IgnoreExt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>
                </StackPanel>
            </GroupBox>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource ModulesTab_AddModule}" Width="120" Margin="0,0,10,0" Command="{Binding AddModuleCommand}"/>
                <Button Content="{DynamicResource ModulesTab_SaveChanges}" Width="120" Margin="0,0,10,0" Command="{Binding SaveModuleCommand}"/>
                <Button Content="{DynamicResource ModulesTab_DeleteModule}" Width="120" Command="{Binding DeleteModuleCommand}"/>
            </StackPanel>
            <TextBlock Text="{DynamicResource ModulesTab_InfoText}" TextWrapping="Wrap" Foreground="Gray" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
